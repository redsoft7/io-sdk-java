# io-sdk-java
Java importer

### Run test

* Start local mysql  
`cd utils/mysql`  
`docker-compose up -d`
* Run test  
`gradlew test`
* Stop local mysql  
`docker-compose down -v`

### Build IO-SDK action for Mysql

`./build.sh mysql`


